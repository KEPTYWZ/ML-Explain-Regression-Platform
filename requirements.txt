# ============================================
# 混凝土强度预测 - Python依赖包
# ============================================
# 
# 重要提示：
# 1. 推荐使用 Python 3.8-3.11 版本
# 2. numpy版本必须 < 2.0，否则会与streamlit等包冲突
# 3. 建议使用虚拟环境安装，避免影响其他项目
#
# 安装方法：
#   pip install -r requirements.txt
#
# ============================================

# 核心依赖（必需）
streamlit>=1.28.0,<2.0.0
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0          # 重要：必须 < 2.0
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0
scikit-learn>=1.3.0,<2.0.0
plotly>=5.18.0

# 机器学习模型
lightgbm>=4.0.0,<5.0.0
xgboost>=2.0.0,<4.0.0

# 超参数优化
bayesian-optimization>=1.4.0

# 模型可解释性
shap>=0.42.0,<0.45.0          # 重要：使用0.42-0.44版本，兼容numpy<2.0

# 符号回归
gplearn>=0.4.2
# pysr>=0.16.0                # 可选：需要Julia环境，不推荐初学者安装

# 数据处理
openpyxl>=3.1.0               # Excel文件支持
pyarrow>=12.0.0,<15.0.0       # Parquet文件支持

# 统计分析
scipy>=1.11.0,<2.0.0
statsmodels>=0.14.0

# ============================================
# 测试通过的版本组合（推荐）
# ============================================
# Python: 3.11
# streamlit: 1.30.0
# numpy: 1.26.4
# pandas: 2.0.3
# scikit-learn: 1.3.2
# lightgbm: 4.6.0
# shap: 0.42.0
# ============================================
