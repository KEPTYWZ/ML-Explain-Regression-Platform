# 🚀 使用说明

## 📋 运行前准备

### 1. 确保已安装Python
- **Python版本：3.8 - 3.11**（推荐3.11）
- ⚠️ **不支持Python 3.12+**（部分依赖包尚未兼容）
- 检查Python版本：打开命令行输入 `python --version`

### 2. 确保已安装pip
- 检查pip：打开命令行输入 `pip --version`

### 3. 重要的版本兼容性说明 ⚠️

**关键依赖版本要求：**
- **numpy < 2.0**（必须！numpy 2.x会导致streamlit崩溃）
- **shap 0.42-0.44**（shap 0.45+要求numpy 2.x，不兼容）
- **streamlit < 2.0**（streamlit 2.x尚未稳定）

**已测试通过的版本组合：**
```
Python: 3.11
streamlit: 1.30.0
numpy: 1.26.4
pandas: 2.0.3
scikit-learn: 1.3.2
lightgbm: 4.6.0
shap: 0.42.0
```

---

## 🔧 安装步骤

### 方式一：使用批处理脚本（推荐，适用于Windows）

**第一步：安装依赖**
- 双击 `install.bat` 文件
- 等待所有依赖包安装完成（可能需要几分钟）

**第二步：运行程序**
- 双击 `run.bat` 文件
- 浏览器会自动打开应用界面（地址：http://localhost:8501）

### 方式二：使用命令行

**第一步：打开命令行**
- 按 `Win + R`，输入 `cmd`，回车
- 使用 `cd` 命令进入项目文件夹
  ```bash
  cd 你的项目路径
  ```

**第二步：安装依赖**
```bash
pip install -r requirements.txt
```

**第三步：运行程序**
```bash
streamlit run app.py
```

**第四步：打开浏览器**
- 浏览器会自动打开
- 如果没有自动打开，手动访问：http://localhost:8501

---

## 📊 使用流程

### 1️⃣ 数据导入
- 点击"浏览文件"按钮
- 选择你的CSV数据文件（支持各种回归分析数据）
- 系统会自动预览数据

**支持的数据类型：**
- 混凝土材料性能数据
- 结构承载力数据
- 工程参数数据
- 任何数值型回归数据

### 2️⃣ 数据预处理
- 选择目标变量（要预测的列）
- 系统自动处理缺失值和异常值
- 查看数据分布和相关性

### 3️⃣ 模型训练
- 选择机器学习模型（RF、SVR、MLP、LightGBM）
- 调整超参数（可选）
- 点击"开始训练"
- 查看模型性能对比

### 4️⃣ 符号回归（可选）
- 使用GP或PySR发现数学公式
- 自动推导预测公式
- 查看公式和性能指标

### 5️⃣ 模型解释
- SHAP分析：查看特征重要性
- ICE图：查看特征影响
- 双因素分析：查看特征交互

---

## ❓ 常见问题

### Q1: 安装时出现numpy版本冲突？
**问题描述：**
```
ERROR: shap requires numpy>=2, but you have numpy 1.26.4
ERROR: streamlit requires numpy<2, but you have numpy 2.4.6
```

**解决方法：**
这是正常的警告！按以下步骤修复：
```bash
pip install numpy==1.26.4 --force-reinstall
pip install shap==0.42.0 --force-reinstall
```

**原因：**
- streamlit需要numpy < 2.0
- 新版shap需要numpy >= 2.0
- 我们使用shap 0.42版本来兼容numpy 1.x

### Q2: 运行时提示"numpy.core.multiarray failed to import"？
**解决方法：**
```bash
pip install numpy==1.26.4 --force-reinstall
```
这是因为numpy版本过高（2.x）导致的。

### Q3: 双击run.bat没反应？
**解决方法：**
1. 确保已经运行过 `install.bat` 安装依赖
2. 右键点击 `run.bat`，选择"以管理员身份运行"
3. 或者使用命令行方式运行

### Q4: 提示"找不到模块"错误？
**解决方法：**
```bash
pip install -r requirements.txt
```
重新安装所有依赖

### Q5: 浏览器没有自动打开？
**解决方法：**
手动打开浏览器，访问：http://localhost:8501

### Q6: 端口8501被占用？
**解决方法：**
```bash
streamlit run app.py --server.port 8502
```
使用其他端口运行

### Q7: 如何停止程序？
**解决方法：**
- 在命令行窗口按 `Ctrl + C`
- 或直接关闭命令行窗口

---

## 📁 数据格式要求

### CSV文件格式
- 第一行必须是列名
- 数据必须是数值型
- 支持中文列名
- 编码：UTF-8 或 GBK

### 示例数据格式
```
水泥,矿渣,粉煤灰,用水量,减水剂,粗骨料,细骨料,龄期,抗压强度
540,0,0,162,2.5,1040,676,28,79.99
540,0,0,162,2.5,1055,676,28,61.89
332.5,142.5,0,228,0,932,594,270,40.27
```

---

## 🛠️ 特征选择工具

### 独立使用feature_selector.py

**命令行运行：**
```bash
python feature_selector.py --help
```

**详细说明：**
查看 `README_feature_selector.md` 文件

---

## 💡 提示

1. **首次运行**：第一次运行可能需要下载一些模型文件，请耐心等待
2. **数据大小**：建议数据量在1000-10000行之间，效果最佳
3. **特征数量**：建议特征数量在5-30个之间
4. **保存结果**：训练完成后可以下载模型和结果
5. **PySR功能**：PySR需要额外安装Julia环境，如不需要可跳过

---

## 📞 获取帮助

如果遇到问题：
1. 查看本文档的"常见问题"部分
2. 查看 `README.md` 了解更多功能
3. 检查命令行的错误信息
4. 确保Python和所有依赖都正确安装

---

**祝使用愉快！** 🎉
