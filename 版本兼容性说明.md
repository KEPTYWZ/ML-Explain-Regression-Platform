# 📦 版本兼容性说明

## ⚠️ 重要提示

本项目对依赖包版本有严格要求，请务必按照以下说明安装。

---

## 🎯 核心版本要求

### Python版本
- ✅ **推荐：Python 3.11**
- ✅ 支持：Python 3.8、3.9、3.10、3.11
- ❌ **不支持：Python 3.12+**（部分依赖包尚未兼容）

### 关键依赖版本限制

| 包名 | 版本要求 | 原因 |
|------|---------|------|
| **numpy** | **< 2.0** | numpy 2.x与streamlit、pyarrow等包不兼容 |
| **shap** | **0.42-0.44** | shap 0.45+要求numpy 2.x |
| **streamlit** | **< 2.0** | streamlit 2.x尚未稳定 |
| **pyarrow** | **< 15.0** | 高版本可能与numpy 1.x冲突 |

---

## ✅ 已测试通过的版本组合

### 推荐配置（最稳定）

```
Python: 3.11.0
streamlit: 1.30.0
numpy: 1.26.4
pandas: 2.0.3
matplotlib: 3.8.0
seaborn: 0.12.2
scikit-learn: 1.3.2
lightgbm: 4.6.0
xgboost: 3.2.0
shap: 0.42.0
scipy: 1.11.0
pyarrow: 14.0.2
```

### 最小版本要求

```
Python: 3.8+
streamlit: 1.28.0
numpy: 1.24.0
pandas: 2.0.0
scikit-learn: 1.3.0
```

---

## 🚨 常见版本冲突问题

### 问题1：numpy版本冲突

**症状：**
```
ERROR: shap requires numpy>=2, but you have numpy 1.26.4
ERROR: streamlit requires numpy<2, but you have numpy 2.4.6
```

**原因：**
- streamlit需要numpy < 2.0
- 新版shap（0.45+）需要numpy >= 2.0
- 这是一个无解的冲突

**解决方案：**
```bash
pip install numpy==1.26.4 --force-reinstall
pip install shap==0.42.0 --force-reinstall
```

### 问题2：运行时崩溃

**症状：**
```
AttributeError: _ARRAY_API not found
ImportError: numpy.core.multiarray failed to import
```

**原因：**
numpy版本过高（2.x），导致用numpy 1.x编译的包无法运行

**解决方案：**
```bash
pip install numpy==1.26.4 --force-reinstall
```

### 问题3：pyarrow导入失败

**症状：**
```
ImportError: pyarrow.lib failed to import
```

**原因：**
pyarrow版本与numpy不兼容

**解决方案：**
```bash
pip install pyarrow==14.0.2 --force-reinstall
```

---

## 💡 安装建议

### 方案一：使用requirements.txt（推荐）

```bash
pip install -r requirements.txt
```

requirements.txt已经指定了兼容的版本范围。

### 方案二：使用虚拟环境（最佳实践）

**Windows:**
```bash
python -m venv ml_env
ml_env\Scripts\activate
pip install -r requirements.txt
```

**Linux/Mac:**
```bash
python -m venv ml_env
source ml_env/bin/activate
pip install -r requirements.txt
```

### 方案三：使用conda环境

```bash
conda create -n ml_project python=3.11
conda activate ml_project
pip install -r requirements.txt
```

---

## 🔍 版本检查命令

安装完成后，可以使用以下命令检查版本：

```bash
python -c "import numpy; print('numpy:', numpy.__version__)"
python -c "import streamlit; print('streamlit:', streamlit.__version__)"
python -c "import shap; print('shap:', shap.__version__)"
python -c "import pandas; print('pandas:', pandas.__version__)"
```

**预期输出：**
```
numpy: 1.26.4
streamlit: 1.30.0
shap: 0.42.0
pandas: 2.0.3
```

---

## 📝 为什么有这些限制？

### NumPy 2.0的重大变化

NumPy 2.0是一个重大版本更新：
- 改变了C API接口
- 改变了内部数据结构
- 用NumPy 1.x编译的包无法在NumPy 2.x下运行

### 依赖链问题

```
streamlit → pyarrow → numpy < 2.0
shap (0.45+) → numpy >= 2.0
```

这是一个无法同时满足的依赖冲突。

### 解决方案

使用shap 0.42-0.44版本，它兼容numpy 1.x。

---

## 🆘 获取帮助

如果遇到版本相关问题：

1. 查看本文档的"常见版本冲突问题"部分
2. 查看 `使用说明.md` 的常见问题部分
3. 确保Python版本在3.8-3.11之间
4. 使用虚拟环境隔离依赖
5. 严格按照requirements.txt安装

---

**最后更新：2026-05-24**
